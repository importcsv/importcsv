# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

_file_map = {

    "clients.baml": "// Learn more about clients at https://docs.boundaryml.com/docs/snippets/clients/overview\n\nclient<llm> CustomGPT4o {\n  provider openai\n  options {\n    model \"gpt-4o\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CustomGPT4oMini {\n  provider openai\n  retry_policy Exponential\n  options {\n    model \"gpt-4o-mini\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CustomSonnet {\n  provider anthropic\n  options {\n    model \"claude-3-5-sonnet-20241022\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n\nclient<llm> CustomHaiku {\n  provider anthropic\n  retry_policy Constant\n  options {\n    model \"claude-3-haiku-20240307\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/round-robin\nclient<llm> CustomFast {\n  provider round-robin\n  options {\n    // This will alternate between the two clients\n    strategy [CustomGPT4oMini, CustomHaiku]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/fallback\nclient<llm> OpenaiFallback {\n  provider fallback\n  options {\n    // This will try the clients in order until one succeeds\n    strategy [CustomGPT4oMini, CustomGPT4oMini]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/retry\nretry_policy Constant {\n  max_retries 3\n  // Strategy is optional\n  strategy {\n    type constant_delay\n    delay_ms 200\n  }\n}\n\nretry_policy Exponential {\n  max_retries 2\n  // Strategy is optional\n  strategy {\n    type exponential_backoff\n    delay_ms 300\n    multiplier 1.5\n    max_delay_ms 10000\n  }\n}",
    "generators.baml": "// This helps use auto generate libraries you can use in the language of\n// your choice. You can have multiple generators if you use multiple languages.\n// Just ensure that the output_dir is different for each generator.\ngenerator target {\n    // Valid values: \"python/pydantic\", \"typescript\", \"ruby/sorbet\", \"rest/openapi\"\n    output_type \"python/pydantic\"\n\n    // Where the generated code will be saved (relative to baml_src/)\n    output_dir \"../\"\n\n    // The version of the BAML package you have installed (e.g. same version as your baml-py or @boundaryml/baml).\n    // The BAML VSCode extension version should also match this version.\n    version \"0.205.0\"\n\n    // Valid values: \"sync\", \"async\"\n    // This controls what `b.FunctionName()` will be (sync or async).\n    default_client_mode sync\n}\n",
    "resume.baml": "// Defining a data model.\nclass Resume {\n  name string\n  email string\n  experience string[]\n  skills string[]\n}\n\n// Create a function to extract the resume from a string.\nfunction ExtractResume(resume: string) -> Resume {\n  // Specify a client as provider/model-name\n  // you can use custom LLM params with a custom client name from clients.baml like \"client CustomHaiku\"\n  client \"openai/gpt-4o\" // Set OPENAI_API_KEY to use this client.\n  prompt #\"\n    Extract from this content:\n    {{ resume }}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n\n\n// Test the function with a sample resume. Open the VSCode playground to run this.\ntest vaibhav_resume {\n  functions [ExtractResume]\n  args {\n    resume #\"\n      Vaibhav Gupta\n      vbv@boundaryml.com\n\n      Experience:\n      - Founder at BoundaryML\n      - CV Engineer at Google\n      - CV Engineer at Microsoft\n\n      Skills:\n      - Rust\n      - C++\n    \"#\n  }\n}\n",
    "transformation.baml": "// Data transformation functions for ImportCSV\n\n// ============== Data Models ==============\n\n// Validation rule structure for different data types\nclass ValidationRule {\n  type string\n  rule string\n  pattern string?\n  valid_examples string[]\n  invalid_examples string[]\n  common_fixes map<string, string>\n}\n\n// Error information from validation\nclass ValidationError {\n  row_index int\n  column_key string\n  value string\n  error_message string\n}\n\n// Row data structure\nclass RowData {\n  row_index int\n  data map<string, string>\n}\n\n// Single transformation\nclass Transformation {\n  row_index int\n  column string\n  old_value string\n  new_value string\n  confidence float\n}\n\n// Result containing all transformations\nclass TransformationResult {\n  transformations Transformation[]\n}\n\n// ============== Functions ==============\n\n// Function to identify relevant columns for transformation\nfunction IdentifyRelevantColumns(\n  prompt: string,\n  available_columns: string[]\n) -> string[] {\n  client \"openai/gpt-4.1\"\n  prompt #\"\n    Given this user request: \"{{ prompt }}\"\n    And these available columns: {{ available_columns }}\n\n    Which columns are relevant for this transformation?\n\n    Return ONLY the column names that are needed for the requested transformation.\n    Be selective - only include columns that are directly relevant.\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Main function to fix validation errors with structured rules\n// Note: validation_rules are passed from Python based on the error types detected\n// No additional LLM call needed - rules are predefined in the application\nfunction FixValidationErrors(\n  user_prompt: string,\n  validation_errors: ValidationError[],\n  row_data: RowData[],\n  validation_rules: map<string, ValidationRule>\n) -> TransformationResult {\n  client \"openai/gpt-4o-mini\"\n  prompt #\"\n    Fix ONLY these rows with validation errors:\n\n    Errors to fix:\n    {{ validation_errors }}\n\n    Data for error rows:\n    {{ row_data }}\n\n    VALIDATION RULES:\n    {% for error_type, rule in validation_rules.items() %}\n    For '{{ error_type }}':\n    - Rule: {{ rule.rule }}\n    - Pattern: {{ rule.pattern }}\n    - Valid examples: {{ rule.valid_examples }}\n    - Invalid examples: {{ rule.invalid_examples }}\n    - Common fixes:\n      {% for pattern, fix in rule.common_fixes.items() %}\n      * {{ pattern }}: {{ fix }}\n      {% endfor %}\n    {% endfor %}\n\n    User request: {{ user_prompt }}\n\n    IMPORTANT:\n    - ONLY fix the rows listed above\n    - Each row should be fixed according to its error message and validation rules\n    - Return transformations ONLY for rows with errors\n    - Do NOT change any other rows\n    - You MUST fix ALL {{ validation_errors | length }} rows listed in the errors above\n    - Generate exactly {{ validation_errors | length }} transformations, one for each error\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// General transformation function for data without validation errors\nfunction TransformDataGeneral(\n  user_prompt: string,\n  row_data: RowData[]\n) -> TransformationResult {\n  client \"openai/gpt-4.1\"\n  prompt #\"\n    Transform this data according to the user's request.\n\n    User request: {{ user_prompt }}\n\n    Data to transform:\n    {{ row_data }}\n\n    Return transformations for the data based on the user's request.\n    Be confident in your transformations and set confidence scores appropriately.\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// ============== Test Wrapper Functions ==============\n// These wrapper functions accept JSON strings for testing purposes\n// They demonstrate how the main functions work with complex data\n\nfunction TestFixValidationErrors(\n  user_prompt: string,\n  validation_errors_json: string,\n  row_data_json: string,\n  validation_rules_json: string\n) -> string {\n  client \"openai/gpt-4.1\"\n  prompt #\"\n    You are testing a data transformation function. Parse the provided JSON data and generate appropriate transformations.\n\n    User request: {{ user_prompt }}\n\n    Validation errors (JSON):\n    {{ validation_errors_json }}\n\n    Row data (JSON):\n    {{ row_data_json }}\n\n    Validation rules (JSON):\n    {{ validation_rules_json }}\n\n    Based on the validation errors and rules provided, generate transformations to fix ONLY the rows with errors.\n\n    Return a JSON object with this structure:\n    {\n      \"transformations\": [\n        {\n          \"row_index\": <number>,\n          \"column\": \"<column_name>\",\n          \"old_value\": \"<original_value>\",\n          \"new_value\": \"<fixed_value>\",\n          \"confidence\": <0.0-1.0>\n        }\n      ]\n    }\n\n    IMPORTANT:\n    - Fix ONLY rows mentioned in validation_errors_json\n    - Use the validation rules to understand how to fix each error\n    - Return valid JSON\n  \"#\n}\n\nfunction TestTransformDataGeneral(\n  user_prompt: string,\n  row_data_json: string\n) -> string {\n  client \"openai/gpt-4o-mini\"\n  prompt #\"\n    You are testing a data transformation function. Parse the provided JSON data and generate transformations.\n\n    User request: {{ user_prompt }}\n\n    Row data (JSON):\n    {{ row_data_json }}\n\n    Generate transformations based on the user's request.\n\n    Return a JSON object with this structure:\n    {\n      \"transformations\": [\n        {\n          \"row_index\": <number>,\n          \"column\": \"<column_name>\",\n          \"old_value\": \"<original_value>\",\n          \"new_value\": \"<transformed_value>\",\n          \"confidence\": <0.0-1.0>\n        }\n      ]\n    }\n  \"#\n}\n\n// ============== Test Cases ==============\n\n// Test the function with a sample transformation request\ntest identify_email_columns {\n  functions [IdentifyRelevantColumns]\n  args {\n    prompt \"Fix all email addresses with an error\"\n    available_columns [\n      \"name\",\n      \"email\",\n      \"phone\",\n      \"address\",\n      \"city\",\n      \"state\",\n      \"zip\",\n      \"country\",\n      \"date_joined\",\n      \"last_login\"\n    ]\n  }\n}\n\n// Test fixing email validation errors using the wrapper function\ntest fix_email_validation_errors {\n  functions [TestFixValidationErrors]\n  args {\n    user_prompt \"Fix the invalid email addresses\"\n    validation_errors_json \"[{\\\"row_index\\\": 0, \\\"column_key\\\": \\\"email\\\", \\\"value\\\": \\\"john[at]example.com\\\", \\\"error_message\\\": \\\"email must be a valid email address\\\"}, {\\\"row_index\\\": 2, \\\"column_key\\\": \\\"email\\\", \\\"value\\\": \\\"jane@sample\\\", \\\"error_message\\\": \\\"email must be a valid email address\\\"}]\"\n    row_data_json \"[{\\\"row_index\\\": 0, \\\"data\\\": {\\\"name\\\": \\\"John Doe\\\", \\\"email\\\": \\\"john[at]example.com\\\"}}, {\\\"row_index\\\": 2, \\\"data\\\": {\\\"name\\\": \\\"Jane Smith\\\", \\\"email\\\": \\\"jane@sample\\\"}}]\"\n    validation_rules_json \"{\\\"email must be a valid email address\\\": {\\\"type\\\": \\\"email\\\", \\\"rule\\\": \\\"Email must contain @ symbol, domain, and TLD\\\", \\\"pattern\\\": \\\"username@domain.tld\\\", \\\"valid_examples\\\": [\\\"user@example.com\\\"], \\\"invalid_examples\\\": [\\\"user[at]example.com\\\", \\\"user@sample\\\"], \\\"common_fixes\\\": {\\\"[at]\\\": \\\"Replace [at] with @\\\", \\\"@sample\\\": \\\"Add domain like @example.com\\\"}}}\"\n  }\n}\n\n// Test general data transformation using the wrapper function\ntest transform_phone_numbers {\n  functions [TestTransformDataGeneral]\n  args {\n    user_prompt \"Format all phone numbers to (XXX) XXX-XXXX format\"\n    row_data_json \"[{\\\"row_index\\\": 0, \\\"data\\\": {\\\"name\\\": \\\"Alice Johnson\\\", \\\"phone\\\": \\\"5551234567\\\"}}, {\\\"row_index\\\": 1, \\\"data\\\": {\\\"name\\\": \\\"Bob Wilson\\\", \\\"phone\\\": \\\"555.234.5678\\\"}}]\"\n  }\n}\n",
}

def get_baml_files():
    return _file_map