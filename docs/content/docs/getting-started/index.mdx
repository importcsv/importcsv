---
title: What is ImportCSV?
description: Get started with ImportCSV - a type-safe CSV importer for React
---

# What is ImportCSV?

ImportCSV is a React component that allows your users to import CSV, XLS, and XLSX files into your application with intelligent column mapping, validation, and error handling.

## When to Use ImportCSV

Use ImportCSV when you need to:

- **Import customer data** - Let users bulk upload contacts, leads, or customer records
- **Onboard existing data** - Help users migrate from spreadsheets or other tools
- **Bulk operations** - Enable mass updates, product imports, or inventory management
- **Data collection** - Accept structured data from clients, partners, or team members

## Key Features

- **Type-Safe with Zod** - Define your schema once, get full TypeScript types automatically
- **Intelligent Mapping** - Fuzzy column matching with configurable similarity threshold
- **High Performance** - Handle 10,000+ rows with virtual scrolling and progressive validation
- **Production Ready** - Built-in validation, transformations, and error handling
- **Zero Config** - CSS auto-injected, no separate imports needed
- **Lightweight** - ~68KB gzipped bundle, optimized with Preact
- **AI Features** - Smart mapping and natural language transforms (requires [backend setup](/self-hosting))

## Installation

```bash
npm install @importcsv/react zod
```

### Optional: Excel Support
```bash
npm install xlsx  # For .xls and .xlsx files
```

## Quick Start

Here's a complete example to get you started:

```tsx
'use client'; // Remove if not using Next.js App Router

import { CSVImporter } from '@importcsv/react';
import { z } from 'zod';
import { useState } from 'react';

// Define your data schema
const schema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
  company: z.string().optional()
});

function App() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <>
      <button onClick={() => setIsOpen(true)}>
        Import Customers
      </button>

      <CSVImporter
        modalIsOpen={isOpen}
        modalOnCloseTriggered={() => setIsOpen(false)}
        schema={schema}
        onComplete={(result) => {
          // result.rows is typed: { name: string; email: string; company?: string }[]
          console.log(`Imported ${result.rows.length} customers`);

          // Send to your API
          fetch('/api/customers/import', {
            method: 'POST',
            body: JSON.stringify(result.rows)
          });
        }}
      />
    </>
  );
}
```

### What You Get

✅ **Automatic file parsing** - CSV, TSV, XLS, XLSX
✅ **Smart column mapping** - Fuzzy matching based on column names
✅ **Built-in validation** - Email, required fields, and more
✅ **Large file support** - 10,000+ rows with virtual scrolling
✅ **Full TypeScript types** - Automatic type inference from schema
✅ **Zero configuration** - Works out of the box

> **Want AI-powered features?** For smart AI column mapping and natural language transforms, see the [AI Features guide](/advanced/ai-features) (requires backend setup).

## Framework-Specific Guides

<Cards>
  <Card
    title="Next.js App Router"
    href="/getting-started/nextjs"
    description="Complete Next.js integration guide"
  />
  <Card
    title="React / Vite"
    href="/getting-started/react"
    description="React and Vite setup guide"
  />
  <Card
    title="Handling Data"
    href="/getting-started/handling-data"
    description="Process imported data patterns"
  />
</Cards>

## How It Works

1. **User uploads file** - CSV, XLS, or XLSX
2. **Column mapping** - Fuzzy matching suggests mappings, user can adjust
3. **Validation** - Data validated against your Zod schema
4. **Error handling** - Clear error messages, option to fix or skip invalid rows
5. **Import complete** - Type-safe data returned to your app

## Next Steps

- [Next.js Guide](/getting-started/nextjs) - Complete Next.js integration
- [React Guide](/getting-started/react) - React and Vite setup
- [Configuration](/configuration/basic) - Customize the importer
- [API Reference](/reference/api) - Complete props documentation
